class GlobalStore{constructor(){this.state={version:'1.0.0',sessionId:generateId(),timestamp:new Date().toISOString(),config:{targetHost:'iat.waf.lyric.adp.com',selectedRegions:['NA','EU','APAC']},testResults:{status:'idle',regions:{}},user:null};this.subscribers=[]}getState(){return{...this.state}}set(path,value){const parts=path.split('.');let cur=this.state;for(let i=0;i<parts.length-1;i++){if(!cur[parts[i]])cur[parts[i]]={};cur=cur[parts[i]]}cur[parts[parts.length-1]]=value;this.notify()}subscribe(cb){this.subscribers.push(cb);return()=>{this.subscribers=this.subscribers.filter(c=>c!==cb)}}notify(){this.subscribers.forEach(cb=>cb(this.state))}}window.globalStore=new GlobalStore()